/**
 * Declares CSS module imports so the TypeScript build can type-check component-scoped stylesheet references.
 */
declare module '*.css';

/**
 * Declares PNG asset imports so the TypeScript build can type-check runtime-served logo references.
 */
declare module '*.png';
