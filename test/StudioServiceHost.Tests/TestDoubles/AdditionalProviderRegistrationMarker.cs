namespace StudioServiceHost.Tests.TestDoubles
{
    /// <summary>
    /// Marks an additional provider registration used only by host endpoint tests.
    /// </summary>
    internal sealed class AdditionalProviderRegistrationMarker
    {
        // This marker type intentionally carries no behavior because tests only need a unique generic type argument.
    }
}
